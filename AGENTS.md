# SilentDepth — Codex 常設ルール

## 変更履歴の自動更新

**以下のいずれかに該当する改修を完了したら、必ずその作業の中で変更履歴を更新すること。**

該当する改修:
- 新フェーズ・新機能の追加
- 既存フェーズの仕様変更（state 構造・関数シグネチャ・定数変更を伴うもの）
- 大規模リファクタリング（100行超の書き換え）
- 新しい UI コンポーネントまたはキャンバス描画の追加

対象外（更新不要）:
- バグ修正のみ
- コメント・スタイル調整
- 軽微な数値チューニング

### 更新手順

1. ファイルを開く:
   `/Users/masatoueda/Documents/my-game-silentdepth/docs/spec.html`

2. `<section id="s10">` 内の `<table>` 末尾（`</table>` の直前）に行を追加する:

```html
<tr>
  <td>YYYY-MM-DD</td><td>フェーズまたは機能名</td>
  <td>変更内容の要約（追加・変更した関数名・定数名・state フィールド名を含める）</td>
</tr>
```

3. 日付は作業完了日（JST）を記入する。

4. Phase 名は表内の既存表記に合わせる（例: `Phase 1（TDC）`、`Phase 4` など）。

---

## 日報の書き出し

**ユーザーが「本日作業終了」と発言したとき、または大きな改修の完了時に Obsidian の日付ノートへ追記すること。**

### 書き出し先

```
/Users/masatoueda/Desktop/desktop/Obsidian/MasaObsidian/YYYY-MM-DD.md
```

- ファイルが存在する場合は末尾に追記する。
- ファイルが存在しない場合は新規作成する。
- 日付は作業完了日（JST）。

### フォーマット

```markdown
## [SilentDepth] 作業報告
- 作業: （実装・変更した内容を箇条書きで。関数名・定数名・ファイル名を含める）
- 結果: （成功/失敗、動作確認の有無）
- 次回: （残タスクや次のステップ）
```
